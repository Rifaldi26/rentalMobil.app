#clear cache 
php artisan route:clear && php artisan config:clear && php artisan cache:clear

#update github
git status
git add .
git commit -m "..."
git push origin main

#connect to github
git remote add origin https://github.com/Rifaldi26/rentalMobil.app

#turn on server
php artisan serve
npm run dev

///

php artisan serve --host=0.0.0.0 --port=8000
npm run dev -- --host
ngrok http 8000

#undo project
git clone https://github.com/Rifaldi26/rentalMobil.app.git
composer install
npm install
copy .env.example .env

# Ganti lokasi rental di bagian dashboard users
var RENTAL_LAT = -6.2088;   // ← ganti dengan latitude kantor
var RENTAL_LNG = 106.8456;  // ← ganti dengan longitude kantor